import Vue from 'vue'

// State
const state = {
  // delivery: []
}

// Getters
const getters = {
  // deliveryData: state => {
  //   return state.delivery
  // },
}

// Mutations
const mutations = {
  // deliveryUpdate(state, data) {
  //   state.delivery = data
  // }
}

// Actions
const actions = {}

export default {
  state,
  getters,
  actions,
  mutations,
}
